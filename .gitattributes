# Normalize text files to LF in the repo (consistent across platforms)
* text=auto eol=lf

# Treat these as binary (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
